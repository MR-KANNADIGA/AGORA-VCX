{
  "name": "𝐀𝐆𝐎𝐑𝐀 𝐕𝐂𝐗",
  "description": "Open-Source & Best Music bot to play songs in your Telegram's Group Voice Chat. Powered by Mr and Mrs Agora.",
  "keywords": ["music", "voicechat", "telegram"],
  "repository": "https://github.com/MR-KANNADIGA/VCX",
  "stack": "container",
  "env": {
    "SESSION_NAME": {
      "description": "Pyrogram session string @Agora_SSGENbot",
      "required": true
    },
    "BOT_TOKEN": {
      "description": "A bot token from @BotFather",
      "required": true
    },
    "BOT_NAME": {
      "description": "Your MusicPlayer Bot username without @.",
      "required": false,
      "value": ""
    },
    "API_ID": {
      "description": "App ID from my.telegram.org/apps",
      "required": true
    },
    "API_HASH": {
      "description": "App hash from my.telegram.org/apps",
      "required": true
    },
    "SUDO_USERS": {
      "description": "List of user IDs counted as admin everywhere (separated by space).",
      "required": true
    },
    "MASTER_USERNAME": {
      "description": "Write Ur Username(Without @ Or With @).",
      "required": true
    },
    "DURATION_LIMIT": {
      "description": "Max audio duration limit for downloads (minutes) i suggest 20-30 min.",
      "required": true,
      "value": "30"
    }
  }
}
